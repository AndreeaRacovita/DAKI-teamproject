@{
    ViewBag.Title = "Progress";
}
<div class="profileMenu">
    <ul>
        <li>@Html.ActionLink("Profile", "Profile", "Account")</li>
        <li>@Html.ActionLink("Badges", "Badges", "Account")</li>
        <li class="active">@Html.ActionLink("Progress", "Progress", "Account")</li>
        <li>@Html.ActionLink("Manage", "Manage", "Account")</li>
    </ul>
</div>
<div class="profile">
    <hgroup class="title">
        <h1>@ViewBag.Title</h1>
    </hgroup>
    
</div>